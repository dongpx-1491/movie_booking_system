<tr>
  <td><%= payment.id %></td>
  <td><%= payment.tickets.size %></td>
  <td><%= l(payment.activated_at, format: :details) %></td>
  <td><%= number_to_currency(payment.total_cost) %></td>
  <td><%= link_to t(".details"), order_history_path(payment), class: "a_details" %></td>
</tr>
