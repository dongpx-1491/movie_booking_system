<% if @status %>
  $("#comments_list").html(
    "<%= j render @rates %>"
  );
  $('#rate_comment').val('');
<% else %>
  $("#comment-error").html(
    "<%= j (render("shared/error_messages", object: @rates)) %>"
  );
<% end %>
