<%= form_for [:admin, @category] do |f| %>
  <%= f.label :name, t(".genre") %>
  <%= f.text_field :name, class: "form-control", id: "input-label" %>
  <%= f.submit t ".submit", class: "btn btn-primary" %>
<% end %>
